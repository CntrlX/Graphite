library designLibrary;

import 'package:flutter/material.dart';

Color mainColor = Colors.grey;
EdgeInsets level1Padding = EdgeInsets.all(10);
BorderRadius level1BorderRadius = BorderRadius.circular(14);
BorderRadius level2BorderRadius = BorderRadius.circular(4);
